name: getpaper
channels:
  - conda-forge
  - bioconda
  - pytorch
  - plotly
  - defaults
dependencies:
  - python=3.10
  - setuptools
  - python-dotenv
  - pytorch
  - pdfminer.six
  - jupyterlab #for examples only
  - pip:
      - pycomfort>=0.0.13
      - click
      - scidownl
      - twine
      - python-dotenv
      - unstructured
      - unstructured-inference
      - unstructured[local-inference]
      - unstructured.PaddleOCR
      - Deprecated
      - semanticscholar
      - loguru
      - langchain
      - pycomfort
      - pdfminer.six
      #- 'git+git+https://github.com/facebookresearch/detectron2.git'
