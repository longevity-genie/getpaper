name: getpaper
channels:
  - conda-forge
  - bioconda
  - pytorch
  - plotly
  - defaults
dependencies:
  - python=3.10
  - setuptools
  - python-dotenv
  - pytorch
  - jupyterlab #for examples only
  - pip:
      - pyfunctional
      - pycomfort
      - more-itertools
      - pynction
      - click
      - scidownl
      - twine
      - tiktoken
      - openai
      - chromadb
      - python-dotenv
      - langchain>=0.0.202
      - unstructured
      - unstructured-inference
      - unstructured[local-inference]
      - unstructured.PaddleOCR
      - Deprecated
      #- 'git+https://github.com/facebookresearch/detectron2.git@e2ce8dc#egg=detectron2'
